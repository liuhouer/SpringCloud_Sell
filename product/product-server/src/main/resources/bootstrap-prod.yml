eureka:
  client:
    service-url:
      defaultZone: http://aliyun.hellozjf.com:8761/eureka
spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        service-id: CONFIG
        enabled: true
      profile: prod